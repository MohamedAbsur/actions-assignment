name: Hello World Action

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 25ds1000041@ds.study.iitm.ac.in
        run: echo "Hello from GitHub Actions!"

      - name: List files
        run: ls -la
